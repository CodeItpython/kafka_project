# 2026-06-13

## 사이드바 토글 애니메이션 보정

- 메뉴 토글을 닫을 때 `menu-pane`이 조건부 렌더링으로 완전히 언마운트되면서 레이아웃이 즉시 재계산되어 새로고침처럼 버벅이는 현상을 확인했다.
- 사이드바를 항상 DOM에 유지하고 `motion.aside`의 `opacity`, `x`, `scale`만 변경하도록 수정했다.
- `chat-shell`은 닫힌 상태에서도 `0 + 본문` grid 구조를 유지하고, 열릴 때만 첫 번째 컬럼과 gap을 부드럽게 확장하도록 변경했다.
- 모바일에서는 사이드바를 fixed overlay로 유지하고 본문은 pointer 이벤트만 비활성화해 화면 전환이 튀지 않도록 조정했다.

## 다음 기술스택 적용 순서

- Redis 캐싱/상태 관리는 이미 일부 적용되어 있으므로 다음 단계에서는 유저 프로필 캐시와 안 읽은 메시지 수를 추가한다.
- 그 다음 MinIO/S3 파일 저장, OpenTelemetry + Prometheus/Grafana, Testcontainers, Elasticsearch 검색 고도화, Debezium Outbox Pattern 순서로 단계적으로 확장한다.
