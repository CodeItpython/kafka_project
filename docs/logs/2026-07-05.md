# 2026-07-05 작업 로그

## 작업 내용

- 00:05 보류했던 백엔드 크리티컬 픽스를 origin/main 기반 새 브랜치(`claude/backend-critical-fixes`)에서 진행 시작
- 00:07 JWT 시크릿 시작 검증 추가 — HS256 기준 32바이트 미만이면 기동 실패, 내장 개발용 기본 시크릿이면 경고 로그(로컬 기본값은 충분히 길어 기동 유지)
- 00:07 스토리지 객체 미존재 시 404 처리: ObjectNotFoundException 도입, S3/로컬 구현체 계약 통일, ApiExceptionHandler 매핑
- 00:08 `spring.h2.console.enabled` 기본값을 `false`로 변경(`SPRING_H2_CONSOLE_ENABLED`로만 활성) — 프로덕션 무인증 H2 콘솔 노출 제거
- 00:10 Redis 원자 카운터(`RedisAtomicCounter`) 도입: INCR + 최초 1회 PEXPIRE를 단일 Lua로 처리. 레이트리밋과 이메일 인증 실패 카운터에 적용해 "EXPIRE 유실로 키가 영구 잔존"하는 경합 제거
- 00:12 레이트리밋 하드닝 — 인증 쓰기 엔드포인트는 Redis 장애 시 fail-closed(503), 그 외는 fail-open 유지. `X-Forwarded-For`는 `app.security.rate-limit.trust-forwarded-for=true`(신뢰 프록시 뒤)일 때만 신뢰하고 기본은 원격 주소 사용
- 00:14 아웃박스 릴레이 트랜잭션 분리 — 배치 단일 트랜잭션에서 이벤트별 `REQUIRES_NEW` + `FOR UPDATE SKIP LOCKED`로 변경. 실패 격리 + 멀티 워커 병렬 처리
- 00:16 통합 테스트(Testcontainers)의 아웃박스 검증을 새 릴레이 API(`findReadyEventIds` + `publishOne`)로 갱신
- 00:24 스토리지 404 처리 단위테스트 추가 (Local/S3 load, ApiExceptionHandler 404 매핑)
- 00:29 프로필 아바타 현대화: 원형+이니셜 중앙정렬, 이름별 색상 그라데이션
- 00:40 프로필 아바타 이니셜 2글자 표시 (크기별 폰트 조정)
- 01:17 채팅방 진입 시 최신 메시지로 스크롤 안 되던 문제 수정(마운트 레이스)
- 01:24 채팅방 목록을 마지막 메시지 시각 기준 정렬(최근 대화 상단): lastMessageAt 도입
- 01:28 사이드바 진입 시 최신 메시지 스크롤 보강: 대화창 애니메이션 완료 시점에 강제 스크롤
- 02:00 (PR #46) 프론트 협업 보안 3건 세트(이메일 6자리 / WebSocket 인가 / 첨부 서명 URL)를 모두 구현하고 앱을 실제 기동해 검증
- 02:05 이메일 인증코드 4자리→6자리: 백엔드 `AuthService`(`%06d`, 10^6 공간) + `AuthDtos` 검증 패턴 `\d{6}`, 프론트 OTP 입력을 상수 `EMAIL_CODE_LENGTH`로 6칸화(입력/붙여넣기/포커스/검증/문구) + CSS 6열, 단위 테스트 기대값 6자리로 갱신
- 02:20 WebSocket 인증/인가: STOMP `ChannelInterceptor` 추가 — CONNECT 프레임의 JWT를 REST와 동일하게 검증해 세션 Principal 설정, SUBSCRIBE 시 방 접근 권한(`ChatRoom#isVisibleTo`)과 알림 토픽 소유(sha256(email))를 검증. 순환 의존 회피용 경량 `ChatRoomAccessService` 분리. 프론트 두 STOMP 클라이언트에 `connectHeaders`로 Bearer 토큰 전달. 인가 단위 테스트 9종
- 02:40 첨부/프로필 이미지 서명 URL: `StorageUrlSigner`(HMAC-SHA256 + 만료 12h) 도입. 응답 방출 계층에서만 서명(캐시/저장은 raw 유지), 첨부는 저장 시 raw 정규화, 두 GET 엔드포인트에서 `exp`/`sig` 검증(무효 시 403). 서명기 단위 테스트 7종

## 첨부 서명 URL 설계·검증

`<img src>`는 Authorization 헤더를 못 실어 JWT 대신 HMAC 서명+만료를 URL에 붙였다(`?exp&sig`). 계층 흐름(엔티티→캐시 스냅샷→응답)에서 이중 서명·캐시 오염을 피하려 **응답 DTO 구성 계층에서만 서명**하고 캐시/DB에는 raw 경로를 저장한다. 첨부 URL은 클라이언트가 왕복 전송하므로 `publishMessage`에서 저장 전 `rawPath`로 정규화한다.

앱을 실제 기동(bootRun + 로컬 스토리지 + docker compose 인프라)해 실 HTTP로 검증(5/5 통과): 업로드 응답 URL에 `exp`/`sig` 포함 / 서명 URL GET 200 / 무서명 GET 403 / 변조 서명 GET 403(서명이 경로에 바인딩). 주의: 서명 TTL(기본 12h)은 방/프로필 Redis 캐시 TTL(30s/5분)보다 길어야 캐시된 서명이 유효하다.

## 개념

- HS256 키 길이: HMAC-SHA256은 최소 256비트(32바이트) 키가 필요하다. 기본/약한 시크릿이 프로덕션에 그대로 나가면 누구나 ADMIN 토큰을 위조할 수 있으므로, 기동 시 길이를 강제하고 개발 기본값 사용은 경고한다. (이 코드베이스의 JWT 검증은 헤더 alg를 신뢰하지 않고 항상 HMAC-SHA256으로 재계산·상수시간 비교하므로 alg-confusion 위험은 없다 — 문제는 시크릿 관리였다.)
- 원자적 카운터: `INCR` 후 별도 `EXPIRE`는 두 번의 왕복이라 사이에 장애가 나면 TTL 없는 키가 남아 버킷이 영구히 막힌다. Lua로 두 명령을 한 번에 실행하면 카운터 생성과 TTL 설정이 원자적으로 보장된다.
- Fail-open vs fail-closed: 가용성 우선 경로(일반 API)는 레이트리밋 백엔드 장애 시 요청을 통과시키고, 브루트포스 방어가 핵심인 인증 쓰기 경로는 차단(fail-closed)한다.
- SKIP LOCKED 아웃박스 릴레이: `SELECT ... FOR UPDATE SKIP LOCKED`는 다른 트랜잭션이 잠근 행을 건너뛰고 잠기지 않은 행만 가져온다. 이벤트별 짧은 트랜잭션에서 잠그고 전송하면 여러 릴레이 워커가 겹치지 않는 이벤트를 동시에 처리한다. 소비자는 message id 기준 멱등이라 at-least-once로 안전하다.
- STOMP 채널 인터셉터 인증: WebSocket 핸드셰이크는 열어두고 STOMP CONNECT 프레임의 네이티브 헤더로 JWT를 받아 인증한다. 인바운드 채널 인터셉터에서 `MessageHeaderAccessor.getAccessor`(wrap 아님 — wrap은 복사본이라 setUser가 세션에 전파되지 않음)로 가변 접근자를 얻어 CONNECT에서 `setUser`하면 이후 SUBSCRIBE 프레임에서 `getUser()`로 주체를 얻는다.
- SUBSCRIBE 인가: 브로드캐스트 토픽(`/topic/rooms/{id}`, `/topic/notifications/{hash}`)은 아무나 구독하면 도청이 되므로, 방 토픽은 방 접근 권한을, 알림 토픽은 sha256(본인 이메일) 일치를 검증한다.
- 6자리 코드: 인증코드 공간을 10^4→10^6으로 넓혀 코드당 브루트포스 성공 확률을 100분의 1로 낮춘다. 프론트 OTP UI와 백엔드 생성·검증 패턴을 함께 변경해야 한다.
